# Native design system

React Native implementation of the Turbo Payload visual language. It shares
semantic colors and component concepts with `@repo/design-system` while using
native primitives instead of DOM-based shadcn/KIBO components.
